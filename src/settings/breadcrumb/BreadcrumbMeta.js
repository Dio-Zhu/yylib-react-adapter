import {MetaType} from '../../index-deps';
module.exports = function(options) {
	var PropMeta = [
	{
	    name: 'separator',
	    label: '分隔符',
	    type: MetaType.Text,
	    props: {},
	    defaultValue: ''
	}];
	return PropMeta;
}